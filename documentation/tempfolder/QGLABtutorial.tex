% This LaTeX was auto-generated from MATLAB code.
% To make changes, update the MATLAB code and export to LaTeX again.

\documentclass{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{graphicx}
\usepackage{color}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{epstopdf}
\usepackage[table]{xcolor}
\usepackage{matlab}
\usepackage[paperheight=795pt,paperwidth=614pt,top=72pt,bottom=72pt,right=72pt,left=72pt,heightrounded]{geometry}

\sloppy
\epstopdfsetup{outdir=./}
\graphicspath{ {./QGLABtutorial_media/} }

\begin{document}

\label{T_0E732DD1}
\matlabtitle{QGLAB Tutorial}

\begin{par}
\begin{flushleft}
Roy H. Goodman (NJIT), Gracie Conte (JHU-APL), and Jeremy Marzuola (UNC)
\end{flushleft}
\end{par}

\label{H_F9B5CEC4}
\matlabheading{Introduction}

\begin{par}
\begin{flushleft}
This tutorial gives a short introduction QGLAB, a MATLAB package for solving PDEs posed on quantum graphs. The software is available from this \href{https://github.com/manroygood/Quantum-Graphs}{GitHub repository} and is described in more detail in this \href{https://arxiv.org/abs/2401.00561}{preprint}. Both the software repository and the preprint feature extensive instructions and examples, a few of which form the basis for this tutorial.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
First, we should define what we mean by quantum graphs. The name is perhaps misleading. The phrase originally referred to metric graphs on which a Schrodinger operator has been defined, but has since come to refer to any problem on which a differential equation is solved on the \textit{edges} of a graph, subject to compatibility conditions at the edges. A \textbf{metric graph} is a graph $\Gamma$ consisting of a set $\mathcal{V}=\lbrace v_1 ,\ldots,v_n \rbrace$of vertices and a set$\mathcal{E}=\lbrace e_1 ,\ldots,{e_m }\rbrace$of directed edges. Several examples are plotted in the next section. Each edge $e_j$ can be thought of a line segment of length $\ell_j$beginning at one vertex $v_{j_1 }$ and ending at a vertex $v_{j_2 }$. We may place a coordinate $0<x<\ell_j$ on this edge, and define a function $\psi_j (x)$ on it, and therefore along the whole metric graph $\Psi =(\psi_1 ,\ldots,\psi_m )$. To define a function space and differential operators, we must specify vertex conditions. The most common are Neumann-Kirchhoff conditions consisting of the \textit{continuity condition}
\end{flushleft}
\end{par}

\begin{par}
\begin{center}
$\Psi (v_k )\equiv \psi_i (v_k )=\psi_j (v_k ),\forall e_i ,e_j \in {\mathcal{V}}_k$,
\end{center}
\end{par}

\begin{par}
\begin{flushleft}
where ${\mathcal{V}}_k$ is the set of edges connected to vertex $v_k$ and \textit{zero flux condition}
\end{flushleft}
\end{par}

\begin{par}
\begin{center}
$\sum_{e_m \in {\mathcal{V}}_k } w_m \psi_m^{\prime } (v_k )=0$.
\end{center}
\end{par}

\begin{par}
\begin{flushleft}
Here the derivative is taken assuming that $v_k$ is the initial point of edge $e_m$ so that $x$ is increasing. This latter condition may be replaced with a Dirichlet condition or a Robin-type condition.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
Our preprint contains an extensive list of references discussing quantum graphs. For the field to grow, researchers must be able to efficiently and accurately set up and common types of problems on quantum graphs and visualize their solutions. We wrote QGLAB to meet this need. 
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
MATLAB provides a built-in directed graph class and a number of functions implementing common graph computations. QGLAB extends this capability to define a quantum graph object and overloads many MATLAB functions such as \texttt{eigs}, \texttt{plot} and \texttt{spy} to work seamlessly with these objects.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
The most important mathematical object defined by QGLAB is a discretized Laplacian operator. We implement this using a design philosophy espoused by Aurentz and Trefethen (block operators) that handles differential equations at points on the interiors of the edges separately from the vertex conditions. Edge $e_m$ is discretized at $N_m +2$ points, $N_m$ of which can be considered as interior points, with the last two considered boundary points. The fundamental idea is to construct a second derivative matrix of dimension $N_m \times (N_m +2)$. Thus $N_m$ equations are used to define the differential equation and $2$ are used to discretize the vertex conditions.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
This requires us to three matrices: the discretized Laplacian $L_{{\mathrm{i}\mathrm{n}\mathrm{t}}}$ and an interpolation matrix $P_{{\mathrm{i}\mathrm{n}\mathrm{t}}}$ that represent equations defined at points on the edges' interiors a vertex condition matrix, and an interpolation matrix $M_{{\mathrm{V}\mathrm{C}}}$. We have implemented both second-order centered-difference discretization and a spectrally accurate Chebyshev collocation approximation. The block operators make it simple to work with either discretization and to switch between them.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
We first describe the basics of using MATLAB's graph capabilities. Next we describe how to define a quantum graph using QGLAB. Finally we solve a few example problems:
\end{flushleft}
\end{par}


\vspace{1em}
\label{H_3D7CE06D}
\matlabheadingtwo{MATLAB graph basics}

\begin{par}
\begin{flushleft}
MATLAB has built-in data types for graphs and directed graphs. We have used the latter as the basic building block for a set of routines (listed in accompanying \texttt{Contents.m} file) for computing with linear and nonlinear quantum graphs: directed graphs which solve PDE's on the edges, subject to some kind of boundary condition or matching condition at the vertices.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
We first show how to create and display a graph:
\end{flushleft}
\end{par}

\begin{matlabcode}
sources=[1 1 2 1 3];
targets=[1 2 2 3 2];
G = graph(sources,targets)
\end{matlabcode}
\begin{matlaboutput}
G = 
  graph with properties:

    Edges: [5x1 table]
    Nodes: [3x0 table]

\end{matlaboutput}
\begin{matlabcode}
plot(G)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_0.png}
\end{center}

\begin{par}
\begin{flushleft}
The input consists of two integer-valued vectors of equal length, from which the graph is built. Each element of each list give the beginning and ending vertex of the corresponding edge. MATLAB refers to these as the source and target nodes of the given edge. The digraph type differs from the graph type in that each edge is directed from the source to the target. It is digraphs that we use to build quantum graphs.
\end{flushleft}
\end{par}

\begin{matlabcode}
DG= digraph(sources,targets)
\end{matlabcode}
\begin{matlaboutput}
DG = 
  digraph with properties:

    Edges: [5x1 table]
    Nodes: [3x0 table]

\end{matlaboutput}
\begin{matlabcode}
plot(DG)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_1.png}
\end{center}

\begin{par}
\begin{flushleft}
Note that the two data structures appear the same, but MATLAB does treat them differently and has some distinct commands for dealing with the two of them. For example the function \texttt{degree} calculates the degree of a node in a graph, but does not work on digraph, which has separate functions \texttt{indegree} and \texttt{outdegree} that compute incoming and outgoing degrees. Let's look at the \texttt{Edges} component:
\end{flushleft}
\end{par}

\begin{matlabcode}
DG.Edges
\end{matlabcode}
\begin{matlabtableoutput}
{
\begin{tabular} {|c|c|c|}\hline
\mlcell{ } & \multicolumn{2}{|c|}{\mlcell{EndNodes}} \\ \hline
\mlcell{ } & \mlcell{1} & \mlcell{2} \\ \hline
\mlcell{1} & \mlcell{1} & \mlcell{1} \\ \hline
\mlcell{2} & \mlcell{1} & \mlcell{2} \\ \hline
\mlcell{3} & \mlcell{1} & \mlcell{3} \\ \hline
\mlcell{4} & \mlcell{2} & \mlcell{2} \\ \hline
\mlcell{5} & \mlcell{3} & \mlcell{2} \\ 
\hline
\end{tabular}
}
\end{matlabtableoutput}

\begin{par}
\begin{flushleft}
Both the \texttt{Edges} and \texttt{Nodes} components are defined as MATLAB \texttt{tables}. At this point the \texttt{Edges} component has one entry called \texttt{EndNodes}. The graph is a purely topological entity. The lengths of the displayed edges are arbitrary. The real usefulness of the data structure is that we can add additional data to both the edges and the nodes. For example we can associate the elements of a vector \texttt{L} to each edge, so long as \texttt{length(L) = length(EndNodes)}.
\end{flushleft}
\end{par}

\begin{matlabcode}
DG.Edges.L=[1;3;5;7;9];
DG.Edges
\end{matlabcode}
\begin{matlabtableoutput}
{
\begin{tabular} {|c|c|c|c|}\hline
\mlcell{ } & \multicolumn{2}{|c|}{\mlcell{EndNodes}} & \mlcell{L} \\ \hline
\mlcell{ } & \mlcell{1} & \mlcell{2} & \mlcell{ } \\ \hline
\mlcell{1} & \mlcell{1} & \mlcell{1} & \mlcell{1} \\ \hline
\mlcell{2} & \mlcell{1} & \mlcell{2} & \mlcell{3} \\ \hline
\mlcell{3} & \mlcell{1} & \mlcell{3} & \mlcell{5} \\ \hline
\mlcell{4} & \mlcell{2} & \mlcell{2} & \mlcell{7} \\ \hline
\mlcell{5} & \mlcell{3} & \mlcell{2} & \mlcell{9} \\ 
\hline
\end{tabular}
}
\end{matlabtableoutput}

\begin{par}
\begin{flushleft}
The Nodes table is initially empty
\end{flushleft}
\end{par}

\begin{matlabcode}
DG.Nodes
\end{matlabcode}
\begin{matlaboutput}
ans =

  3x0 empty table
\end{matlaboutput}

\begin{par}
\begin{flushleft}
but we can add data to each node in a similar manner.
\end{flushleft}
\end{par}

\begin{matlabcode}
DG.Nodes.mass = [1;4;9];
DG.Nodes
\end{matlabcode}
\begin{matlabtableoutput}
{
\begin{tabular} {|c|c|}\hline
\mlcell{ } & \mlcell{mass} \\ \hline
\mlcell{1} & \mlcell{1} \\ \hline
\mlcell{2} & \mlcell{4} \\ \hline
\mlcell{3} & \mlcell{9} \\ 
\hline
\end{tabular}
}
\end{matlabtableoutput}


\label{H_B8F3E54F}
\matlabheadingtwo{Graphs MATLAB Can't Work With (version 2017b and earlier).}

\begin{par}
\begin{flushleft}
Before version 2018a, Matlab's \texttt{digraph }structure could not handle multiple edges sharing the same source and target. Therefore a graph such as the \textit{mandarin graph}, which has two vertices, and three separate edges connecting them cannot be represented.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
The following will return an error in MATLAB versions older than 2018a.
\end{flushleft}
\end{par}

\begin{matlabcode}
source=[1 1 1 2]; target=[1 2 2 2];
quantumGraph(source,target,1);
\end{matlabcode}


\label{H_52D3F3BE}
\matlabheadingtwo{Constructing the Quantum Graph}

\begin{par}
\begin{flushleft}
To create the quantum graph routines, we first created a quantum graph class, which is built on top of the digraph class, and added appropriate data for quantum graph problems:
\end{flushleft}
\end{par}

\begin{matlabcode}
source=[1 1 2];
target=[1 2 2];
L = [2*pi,4, 2*pi];
Phi = quantumGraph(source,target,L);
\end{matlabcode}

\begin{par}
\begin{flushleft}
There can be other fields associated with the quantum graph but these are either optional fields, or fields set to the default, and must be set in a slightly different way. 
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
Most of the information is associated with the edges
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{L:} (required)the vector of lengths (if scalar than repeated)
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{nx:} (optional) the number of discretization points on each edge (if scalar then \texttt{nx} is the density of points per unit length and the same value is used on each edge)
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{x} and \texttt{y}: (optional, created if nxVec is set) the independent and dependent variables on each edge. \texttt{y} is set to \texttt{NaN} on initialization. Only created if nxVec specified. If \texttt{length(nxVec)==1}, then it is interpreted as the density of discretizatio points per unit length
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi.Edges
\end{matlabcode}
\begin{matlabtableoutput}
{
\begin{tabular} {|c|c|c|c|c|c|c|c|c|}\hline
\mlcell{ } & \multicolumn{2}{|c|}{\mlcell{EndNodes}} & \mlcell{Weight} & \mlcell{L} & \mlcell{nx} & \mlcell{x} & \mlcell{y} & \mlcell{integrationWeight} \\ \hline
\mlcell{ } & \mlcell{1} & \mlcell{2} & \mlcell{ } & \mlcell{ } & \mlcell{ } & \mlcell{ } & \mlcell{ } & \mlcell{ } \\ \hline
\mlcell{1} & \mlcell{1} & \mlcell{1} & \mlcell{1} & \mlcell{6.2832} & \mlcell{126} & \mlcell{128x1 double} & \mlcell{128x1 double} & \mlcell{0.0499} \\ \hline
\mlcell{2} & \mlcell{1} & \mlcell{2} & \mlcell{1} & \mlcell{4} & \mlcell{80} & \mlcell{82x1 double} & \mlcell{82x1 double} & \mlcell{0.0500} \\ \hline
\mlcell{3} & \mlcell{2} & \mlcell{2} & \mlcell{1} & \mlcell{6.2832} & \mlcell{126} & \mlcell{128x1 double} & \mlcell{128x1 double} & \mlcell{0.0499} \\ 
\hline
\end{tabular}
}
\end{matlabtableoutput}

\begin{par}
\begin{flushleft}
There are two variables associated to the nodes:
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{y}: the dependent variable
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{robinCoeff}: optional the constant determining the Robin boundary condition used in the Laplacian and in interpolating \texttt{y} from the edges to the nodes. This optional argument defaults to zero, i.e. to Kirchoff/Neuman boundary conditions. May be specified as scalar or vector. At leaf nodes only, Dirichlet boundary conditions can be used. This is specified by a \texttt{NaN}. Specifying \texttt{NaN} at a non-leaf node will return an error.
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi.Nodes
\end{matlabcode}
\begin{matlabtableoutput}
{
\begin{tabular} {|c|c|c|}\hline
\mlcell{ } & \mlcell{robinCoeff} & \mlcell{y} \\ \hline
\mlcell{1} & \mlcell{0} & \mlcell{NaN} \\ \hline
\mlcell{2} & \mlcell{0} & \mlcell{NaN} \\ 
\hline
\end{tabular}
}
\end{matlabtableoutput}

\begin{par}
\begin{flushleft}
The above quantum graph is suitable for running the secular determinant codes below. This symbolic calculation does not depend on a discretization. However if you want to do numerics, you will need to set up vectors of \textit{x}- and \textit{y}- values along each edge. This involves first defining a vector \texttt{nx} containing the number of x values along each edge (including the endpoints), and then calling the following two routines to set up fields to hold the independent and dependent coordinates. For now, only uniform discretizations are allowed  but in the future we will add support for Chebyshev discretizations. Note that if \texttt{nxvec} is specificed but \texttt{Discretization} is not, then \texttt{Discretization} defaults to \texttt{Uniform}. The general syntax is now
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
\texttt{dumbbell=quantumGraph(source,target,L,'Key1',value1, 'key2',value2,...)}
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
The three required input parameters must appear in that order, followed by the optional parameters, specified as key-value pairs, that can appear in any order. The optional arguments, for now, are
\end{flushleft}
\end{par}

\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item{\begin{flushleft} \texttt{nxvec }(associated to edges) \end{flushleft}}
   \item{\begin{flushleft} \texttt{robincoeff }(associated to nodes) \end{flushleft}}
   \item{\begin{flushleft} \texttt{Discretization }(property of graph) \end{flushleft}}
   \item{\begin{flushleft} \texttt{plotCoordinateFcn }(property of graph, allows overloading of \texttt{plot)} \end{flushleft}}
\end{itemize}

\begin{par}
\begin{flushleft}
In addition, there are methods to add or change these values.  
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi=quantumGraph(source,target,L,...
    'Discretization','Chebyshev',...
    'nxvec',[63 20 63],...
    'plotCoordinateFcn',@dumbbellPlotCoords);
Phi.plot('layout')
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_2.png}
\end{center}

\begin{par}
\begin{flushleft}
The final name-value pair specifies the name of a user-defined function that sets up the coordinates and enables the plots of eigenfunctions given below. 
\end{flushleft}
\end{par}


\label{H_8300FE72}
\matlabheadingtwo{Template functions}

\begin{par}
\begin{flushleft}
For many of our favorite graphs, we have created template functions, which are stored in the directory \texttt{source/templates}. All we need to do to create a dumbbell matrix is to enter
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi = quantumGraphFromTemplate('star');
Phi.plot('layout')
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_3.png}
\end{center}

\begin{par}
\begin{flushleft}
The template file contains default values for constructing the quantum graph. By default a uniform grid is used with centered difference discretization.  Defaults can be overridden using key-value pairs. Each template constructor contains a program that defines the graph's layout for plotting.
\end{flushleft}
\end{par}


\label{H_527D93F7}
\matlabheadingtwo{Visualizing the quantum graph discretization}

\begin{par}
\begin{flushleft}
As discussed in the introduction, QGLAB uses a block-matrix formulation to discretize differential equations whose space derivatives are given by Laplacians. This requires the construction of three matrices: the discretized Laplacian, an interpolation matrix, and a vertex condition matrix.  These matrices are constructed directly from the quantum graph object. If a discretization is specified, then they are defined when the quantum graph is initially constructed. 
\end{flushleft}
\end{par}

\label{H_AC72B449}
\matlabheadingthree{Finite Difference}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
Phi = quantumGraphFromTemplate('dumbbell');
Phi.spy
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_4.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_5.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_6.png}
\end{center}

\label{H_0F37778E}
\matlabheadingthree{Chebyshev}

\begin{par}
\begin{flushleft}
This, of course, gives higher accuracy at the expense of constructing blockwise-dense matrices.
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi = quantumGraphFromTemplate('dumbbell','Discretization','Chebyshev');
Phi.spy
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_7.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_8.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_9.png}
\end{center}


\label{H_B4668F6C}
\matlabheading{Setting up and solving problems on quantumGraphs}

\label{H_0C33BDEA}
\matlabheadingtwo{A Poisson problem}

\begin{par}
\begin{flushleft}
Set up a quantum graph in the form of a dumbbell plus one extra edge
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
Solve the vertex-value problem 
\end{flushleft}
\end{par}

\begin{par}
$$\begin{array}{l}
\triangle u=f\\
\sum u^{\prime } ({v_1 })+u({v_1 })=1\\
\sum u^{\prime } ({v_2 })+u({v_2 })=2\\
u({v_3 })=3
\end{array}$$
\end{par}

\begin{par}
\begin{flushleft}
where 
\end{flushleft}
\end{par}

\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item{\begin{flushleft} The functions on the right-hand-sides are $f=\left\lbrace \cos x,x,\sin 2x,1\right\rbrace$on the four edges. \end{flushleft}}
   \item{\begin{flushleft} the edges have lengths $\left\lbrace 2\pi ,4,2\pi ,1\right\rbrace$. \end{flushleft}}
   \item{\begin{flushleft} The exact solutions on the edges are $\left\lbrace \frac{19}{2}-\cos x,\frac{1}{6}(51-45x+x^3 ),\frac{1}{12}\left(-130-3\sin 2x\right),\frac{1}{6}(-65+80x+3x^2 )\right\rbrace$. \end{flushleft}}
\end{itemize}

\begin{par}
\begin{flushleft}
The vertex conditions have been generalized from the Kirchhoff-Neumann condition described in the introduction to a metric graph equivalent of Robin boundary condition at vertices 1 and 2, with Robin coefficient one, and the third has a Dirichlet vertex condition which is specified by setting the \texttt{robinCoeff} parameter to \texttt{NaN}. The vertex conditions are nonhomogeneous, which is specified by the variable \texttt{nodeData}.
\end{flushleft}
\end{par}

\label{H_0F991983}
\begin{par}
\begin{flushleft}
First, we define the quantum graph.
\end{flushleft}
\end{par}

\begin{matlabcode}
s=[1 1 2 2];
t=[1 2 2 3];
L=[2*pi 4 2*pi 1];
robinCoeff=[1 1 nan];
\end{matlabcode}

\label{H_CD64EE6C}
\begin{par}
\begin{flushleft}
Define the exact solution
\end{flushleft}
\end{par}

\begin{matlabcode}
u1=@(x)(19/2-cos(x));
u2=@(x)(51-45*x+x.^3)/6;
u3=@(x)(-130-3*sin(2*x))/12;
u4=@(x)(-65+80*x+3*x.^2)/6;
\end{matlabcode}

\label{H_57A620D7}
\begin{par}
\begin{flushleft}
Define the right hand sides
\end{flushleft}
\end{par}

\begin{matlabcode}
f1=@(x)cos(x);
f2=@(x)x;
f3=@(x)sin(2*x);
f4=1;
nodeData = [1;2;3];
\end{matlabcode}

\begin{par}
\begin{flushleft}
Define the quantum graph 
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi=quantumGraph(s,t,L,'RobinCoeff',robinCoeff,'nxVec',40,'Discretization','Chebyshev');
Phi.addPlotCoords(@poissonExamplePlotCoords); % Sets up plotting
Phi.plot('layout')
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_10.png}
\end{center}

\begin{par}
\begin{flushleft}
Set up the exact solution on the edges
\end{flushleft}
\end{par}

\begin{matlabcode}
exactSolution = Phi.applyFunctionsToAllEdges({u1,u2,u3,u4});
\end{matlabcode}

\begin{par}
\begin{flushleft}
Set up the nonhomogeneous data on the edges
\end{flushleft}
\end{par}

\begin{matlabcode}
edgeData = Phi.applyFunctionsToAllEdges({f1,f2,f3,f4});
\end{matlabcode}

\begin{par}
\begin{flushleft}
Solve and compute errors
\end{flushleft}
\end{par}

\begin{matlabcode}
numericalSolution = Phi.solvePoisson('edgeData',edgeData,'nodeData',nodeData);
numericalError= Phi.norm(exactSolution-numericalSolution,inf);
\end{matlabcode}

\begin{par}
\begin{flushleft}
Plot the solution and display the error
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi.plot(numericalSolution)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_11.png}
\end{center}
\begin{matlabcode}
fprintf('The sup-norm of the error is %0.3e.\n',numericalError)
\end{matlabcode}
\begin{matlaboutput}
The sup-norm of the error is 7.679e-11.
\end{matlaboutput}


\label{H_402B04AA}
\matlabheadingtwo{An eigenproblem}

\begin{par}
\begin{flushleft}
We compute the four eigenvalues closest to zero for the dumbbell quantum graph.
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi=quantumGraphFromTemplate('dumbbell','Discretization','Chebyshev');
[V,lambda]=Phi.eigs(4);
for k=1:4
    figure
    Phi.plot(V(:,k))
    title(sprintf('Eigenfunction %i, $\\lambda = %0.3f$', k, lambda(k)));
end
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_12.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_13.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_14.png}
\end{center}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_15.png}
\end{center}

\label{H_10089249}
\matlabheadingthree{The secular determinant}

\begin{par}
\begin{flushleft}
The eigenvalues of quantum graph are given by $\lambda =-k^2$ where $k$ is a root of a function called the \textit{secular determinant}. This function is defined by noting that on each edge, the eigenfunction must be a linear combination of $\sin kx$ and $\cos kx$, and attempting to enforce the vertex conditions. The following code constructs the secular determinant as a symbolic expression $f(k)$. 
\end{flushleft}
\end{par}

\begin{matlabcode}
f=secularDet(Phi)
\end{matlabcode}
\begin{matlabsymbolicoutput}
f = 

\hskip1em $\displaystyle -\frac{{\left(\cos \left(2\,\pi \,x\right)-1\right)}\,{\left(4\,\cos \left(4\,x\right)\,\sin \left(2\,\pi \,x\right)-3\,\sin \left(4\,x\right)+5\,\sin \left(4\,x\right)\,\cos \left(2\,\pi \,x\right)\right)}}{4}$
\end{matlabsymbolicoutput}
\begin{matlabcode}
fplot(f,[0 2*pi])
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_16.png}
\end{center}
\begin{matlabcode}
k=linspace(0,1);
ff=matlabFunction(f);
fk=ff(k);
\end{matlabcode}

\begin{par}
\begin{flushleft}
The smallest root is just $k=0$. Find the next three and see that they agree with the eigenvalues to all digits shown
\end{flushleft}
\end{par}

\begin{matlabcode}
guesses=find(fk(1:end-1).*fk(2:end)<0);
fprintf('Numerical  Analytical')
\end{matlabcode}
\begin{matlaboutput}
Numerical  Analytical
\end{matlaboutput}
\begin{matlabcode}
for i=1:3 
    kstar=fzero(ff,k(guesses(i)));
    fprintf('%2.5f    %2.5f\n',lambda(i+1),-kstar^2)
end
\end{matlabcode}
\begin{matlaboutput}
-0.05857    -0.05857
-0.42970    -0.42970
-0.89706    -0.89706
\end{matlaboutput}


\matlabheadingtwo{Drawing a bifurcation diagram}

\begin{par}
\begin{flushleft}
This code draws the bifurcation diagram from Figure 3.3 of Goodman, R. H. \href{http://doi.org/10.3934/dcds.2019093}{NLS bifurcations on the bowtie combinatorial graph and the dumbbell metric graph.} \textbf{Discrete \& Continuous Dynamical Systems - A,} 39(4), 2203–2232 (2019) plus some extra branches to show how the code works.
\end{flushleft}
\end{par}


\matlabheadingthree{Construct the quantum graph and find the first few eigenfunctions}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
tag='dumbbell';
L = [2*pi 4 2*pi];
nxvec=6;
Phi=quantumGraphFromTemplate(tag,'LVec',L,'nX',nxvec,'discretization','Uniform');
dataDir = makeContinuationDirectory(Phi,tag);
\end{matlabcode}
\begin{matlaboutput}
Created directory data/dumbbell/150.
\end{matlaboutput}

\matlabheadingthree{Compute and save some eigenfunctions}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
nToPlot=4;
nDoubles=1;
saveEigenfunctions(Phi,tag,dataDir,nToPlot,nDoubles);
\end{matlabcode}

\matlabheadingthree{Define the cubic nonlinearity and save it to the data directory. }

\begin{par}
\begin{flushleft}
This must be a symbolic function of a single variable \texttt{z}.
\end{flushleft}
\end{par}

\begin{matlabcode}
syms z
f = 2*z^3;
fcns = saveNLSFunctionsGraph(dataDir,Phi,f);
\end{matlabcode}


\matlabheadingthree{Compute branches that are continuation of the three least oscillatory eigenfunctions}

\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item{\begin{flushleft} The function \texttt{continuerSet} acts like the MATLAB functions \texttt{odeset} and \texttt{optimset.} It sets a number of options that are used by the continuation programs \end{flushleft}}
   \item{\begin{flushleft} The function \texttt{continueFromEig} continues a branch from the linear limit of an eigenfunction computed in the previous step \end{flushleft}}
\end{itemize}

\begin{matlabcode}
options=continuerSet([],'LambdaThresh',-3,'NThresh',9,'plotFlag',false,'minNormDelta',1e-3);
branch1=continueFromEig(dataDir,1,options);
\end{matlabcode}
\begin{matlaboutput}
N threshold crossed.
Branch number 1.
Data saved to directory data/dumbbell/150/branch001.
Branching Bifurcation at solution number 6.
Branching Bifurcation at solution number 9.
Branching Bifurcation at solution number 12.
Branching Bifurcation at solution number 14.
\end{matlaboutput}
\begin{matlabcode}
branch2=continueFromEig(dataDir,2,options);
\end{matlabcode}
\begin{matlaboutput}
Lambda threshold crossed.
Branch number 2.
Data saved to directory data/dumbbell/150/branch002.
No branching bifurcations found.
\end{matlaboutput}
\begin{matlabcode}
branch3=continueFromEig(dataDir,3,options);
\end{matlabcode}
\begin{matlaboutput}
N threshold crossed.
Branch number 3.
Data saved to directory data/dumbbell/150/branch003.
No branching bifurcations found.
\end{matlaboutput}


\matlabheadingthree{Compute some branches that bifurcate from this first branch}

\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item{\begin{flushleft} The function \texttt{continueFromBranchpoint} continues a branch from the first branching bifurcation point. Since this is a symmetry-breaking bfirucation, we only need to continue in one direction, the branch stored in \texttt{branch1} at bifurcation location 1  \end{flushleft}}
\end{itemize}

\begin{matlabcode}
location=1;
direction=1;
branch4=continueFromBranchPoint(dataDir,branch1,location,direction,options);
\end{matlabcode}
\begin{matlaboutput}
Lambda threshold crossed.
Branch number 4.
Data saved to directory data/dumbbell/150/branch004.
No branching bifurcations found.
\end{matlaboutput}


\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item{\begin{flushleft} The function \texttt{continueFromBranchpoint} continues a branch from the first branching bifurcation point. Since this is a transcritical bfirucation, we need to continue in both directions, the branch stored in \texttt{branch1} at bifurcation location 2 \end{flushleft}}
\end{itemize}

\begin{matlabcode}
location=2;
direction=1;
branch5=continueFromBranchPoint(dataDir,branch1,location,direction,options);
\end{matlabcode}
\begin{matlaboutput}
Lambda threshold crossed.
Branch number 5.
Data saved to directory data/dumbbell/150/branch005.
Branching Bifurcation at solution number 21.
\end{matlaboutput}
\begin{matlabcode}
location=2;
direction=-1;
branch6=continueFromBranchPoint(dataDir,branch1,location,direction,options);
\end{matlabcode}
\begin{matlaboutput}
Lambda threshold crossed.
Branch number 6.
Data saved to directory data/dumbbell/150/branch006.
No branching bifurcations found.
\end{matlaboutput}


\matlabheadingthree{Compute a branch that bifurcates from the third branch}

\begin{par}
\begin{flushleft}
Continues from the branch stored in \texttt{branch5} from the bifurcation point 1
\end{flushleft}
\end{par}

\begin{matlabcode}
location=1;
direction=1;
branch7=continueFromBranchPoint(dataDir,branch5,location,direction,options);
\end{matlabcode}
\begin{matlaboutput}
Lambda threshold crossed.
Branch number 7.
Data saved to directory data/dumbbell/150/branch007.
No branching bifurcations found.
\end{matlaboutput}


\matlabheadingthree{Compute a large-amplitude solution, save it to a file, and compute its continuation}

\begin{par}
\begin{flushleft}
This seeds a standing wave with large frequency, localized and with positive amplitudes on edges 1 and 2.
\end{flushleft}
\end{par}

\begin{matlabcode}
Lambda0=-4;
edges=[1 2];
signs=[1 1];
filenumber=saveHighFrequencyStandingWave(dataDir,Lambda0,edges,signs);
\end{matlabcode}
\begin{matlaboutput}
File saved to data/dumbbell/150/savedFunction.001.
File number is 1. 
\end{matlaboutput}
\begin{matlabcode}
options=continuerSet(options,'LambdaThresh',-4.1,'NThresh',9);
direction=-1;
branch8=continueFromSaved(dataDir,filenumber,direction,options);
\end{matlabcode}
\begin{matlaboutput}
N threshold crossed.
Branch number 8.
Data saved to directory data/dumbbell/150/branch008.
No branching bifurcations found.
\end{matlaboutput}
\begin{matlabcode}
bifurcationDiagram(dataDir)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_17.png}
\end{center}


\matlabheadingthree{Plot some solutions}

\begin{par}
\begin{flushleft}
The fifteenth computed solution on branch 4:
\end{flushleft}
\end{par}

\begin{matlabcode}
plotSolution(dataDir,branch4,15)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_18.png}
\end{center}


\begin{par}
\begin{flushleft}
The last computed solution on branch 8:
\end{flushleft}
\end{par}

\begin{matlabcode}
plotSolution(dataDir,branch8,'last')
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_19.png}
\end{center}


\matlabheadingtwo{Heat flow on a dumbbell graph}

\begin{par}
\begin{flushleft}
In this example, we adapt a standard time-stepper, the Crank-Nicholson method to the block matrix formulation. We solve the heat equation $u_t =\triangle u$ on a dumbbell graph.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
The standard Crank-Nicholson method with time step $h$ is:
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
$\left(I-\frac{h}{2}\triangle \right)u_{k+1} =\left(I+\frac{h}{2}\triangle \right)u_k$.
\end{flushleft}
\end{par}

\begin{par}
\begin{flushleft}
We must modify that to ensure that $u_{k+1}$ satisfies the discrete vertex conditions. In the notation of the paper we let
\end{flushleft}
\end{par}

\begin{par}
$$L_- =\binom{P_{{\mathrm{i}\mathrm{n}\mathrm{t}}} -\frac{h}{2}L_{{\mathrm{i}\mathrm{n}\mathrm{t}}} }{M_{{\mathrm{V}\mathrm{C}}} }\;\textrm{and}\;L_+ =\binom{P_{{\mathrm{i}\mathrm{n}\mathrm{t}}} +\frac{h}{2}L_{{\mathrm{i}\mathrm{n}\mathrm{t}}} }{0}$$
\end{par}

\begin{par}
\begin{flushleft}
and then iterate $L_- u_{k+1} =L_+ u_k$. The matrices are defined blockwise, with the upper blocks represent the differential equation on the edges' interiors and the bottom blocks enforce the vertex conditions.
\end{flushleft}
\end{par}

\matlabheadingthree{Set up the graph and the initial condition}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
Phi=quantumGraphFromTemplate('dumbbell');
f0=@(x)(2-2*cos(x-pi/3));
f1=1;
f2=@(x)cos(x);
y=Phi.applyFunctionsToAllEdges({f0,f1,f2});
totalHeatInitial = Phi.integral(y);
figure;Phi.plot(y);
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_20.png}
\end{center}
\begin{matlabcode}

\end{matlabcode}

\matlabheadingthree{Set up the time stepping}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
h=0.01;
tFinal=10;
nStep=tFinal/h;
\end{matlabcode}

\matlabheadingthree{Set up the evolution matrices}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
Lint=Phi.wideLaplacianMatrix;
Pint=Phi.interpolationMatrix;
Lplus=Phi.extendWithZeros(Pint+h/2*Lint);
Lminus=Phi.extendWithVC(Pint-h/2*Lint);
\end{matlabcode}

\matlabheadingthree{Evolve  the solution}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
keepCount=1;
for k=1:nStep
    y = Lplus*y;
    y = Lminus\y;
end
totalHeatFinal = Phi.integral(y);
figure;Phi.plot(y);
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_21.png}
\end{center}

\matlabheadingthree{Show that the total heat is conserved to many digits}

\begin{par}
\hfill \break
\end{par}

\begin{matlabcode}
fprintf('Change in heat is %0.2e.\n',totalHeatFinal-totalHeatInitial);
\end{matlabcode}
\begin{matlaboutput}
Change in heat is -1.58e-12.
\end{matlaboutput}


\matlabheading{KPP evolution on a honeycomb}

\begin{par}
\begin{flushleft}
Evolves a solution to the KPP equation $u_t =\mu \triangle u+u(1-u)$ on a honeycomb graph. Here we evolve the approximate solution using the QGLAB function
\end{flushleft}
\end{par}

\begin{matlabcode}
Phi = hexOfHexes("cellsPerSide",4,"nx",32);
\end{matlabcode}


\matlabheading{Define and plot the initial conditions }

\begin{par}
\begin{flushleft}
We choose an initial condition which is a Gaussian based on the physical coordinates of the points on the graph. This graph has 132 edges and we center the initial condition on the center of edge $e_{53}$.
\end{flushleft}
\end{par}

\begin{matlabcode}
centerEdge=53;
spot=round(Phi.nx(centerEdge)/2);
X1=Phi.Edges.x1{centerEdge}(spot);
X2=Phi.Edges.x2{centerEdge}(spot);
f0 = @(x1,x2)exp(-(x1-X1).^2-(x2-X2).^2)/2;
u0 = Phi.applyGraphicalFunction(f0);
Phi.pcolor(u0)
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_22.png}
\end{center}


\matlabheading{Function Definition and Time evolution}

\begin{par}
$$f(t,u)=\mu \triangle u+F(t,u)$$
\end{par}

\begin{matlabcode}
mu = 0.1; 
F =@(z) z.*(1-z);
tFinal = 12;
dt = 0.1;
[t,u] = Phi.qgdeSDIRK443(mu,F,tFinal,u0,dt,'nSkip',10);
Phi.pcolor(u(:,end))
\end{matlabcode}
\begin{center}
\includegraphics[width=\maxwidth{35.423983943803314em}]{figure_23.png}
\end{center}

\end{document}
